"""
Este archivo contiene todas las funciones del desafio de calentando_motores
"""


def mimir(dia_semana, vacacion):
    """
    El parametro dia_semana es True si estamos en un dia de la semana, el parametro vacacion es True si estamos en vacaciones. Podemos mimir si no es dia de semana o si estamos en vacaciones.
    Devolver True si podemos mimir

    mimir(False, False) → True
    mimir(True, False) → False
    mimir(False, True) → True
    """
    print("not implemented")
